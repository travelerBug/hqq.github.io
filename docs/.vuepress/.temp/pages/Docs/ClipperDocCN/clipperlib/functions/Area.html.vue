<template><div><h3 id="area" tabindex="-1"><a class="header-anchor" href="#area" aria-hidden="true">#</a> <strong>Area</strong></h3>
<div class="language-text ext-text"><pre v-pre class="language-text"><code>Del.» function Area(const pts: TPath): double;

C++ » double Area(const Path &amp;poly);

C#  » public static double Area(Path poly);
</code></pre></div><p>该函数返回多边形的面积（默认输入的多边形为封闭轮廓）。根据方向的不同，该值大小可能为正或者负。如果方向为正，则结果为正，相反的，如果方向为负，则方向为负；</p>
</div></template>
