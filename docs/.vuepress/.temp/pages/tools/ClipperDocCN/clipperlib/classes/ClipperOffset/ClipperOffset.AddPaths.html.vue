<template><div><h3 id="clipperoffset-addpaths" tabindex="-1"><a class="header-anchor" href="#clipperoffset-addpaths" aria-hidden="true">#</a> <strong>ClipperOffset.AddPaths</strong></h3>
<div class="language-text ext-text"><pre v-pre class="language-text"><code>Del.» procedure AddPaths(const Paths: TPaths; JoinType: TJoinType; EndType: TEndType);

C++ » void AddPaths(const Paths&amp; paths, JoinType jointype, EndType endtype);

C#  » public void AddPaths(Paths paths, JoinType jointype, EndType endtype);
</code></pre></div><p>同上页AddPath，为添加路径组；</p>
</div></template>
