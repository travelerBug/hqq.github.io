<template><div><h3 id="clipper-constructor" tabindex="-1"><a class="header-anchor" href="#clipper-constructor" aria-hidden="true">#</a> <strong>Clipper.Constructor</strong></h3>
<div class="language-text ext-text"><pre v-pre class="language-text"><code>Del.» constructor TClipper.Create(InitOptions: TInitOptions = []);

C++ » Clipper::Clipper(int initOptions = 0) : ClipperBase();

C#  » public Clipper(initOptions = 0): base() {};
</code></pre></div><p>构建一个Clipper对象，可以使用InitOptions来进行初始化；</p>
</div></template>
